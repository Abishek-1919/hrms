# Backend

Minimal Node.js service placeholder for future HRMS APIs.

## Run

```bash
npm run dev:backend
```

Health check:

```bash
curl http://localhost:4000/health
```

When the API is implemented, add Express, PostgreSQL access, validation, and tests here.
